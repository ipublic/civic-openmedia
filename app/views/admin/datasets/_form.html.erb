<div id="forms">
	<div class="grid_12">
		<div class="box">
			<h2><%= @dataset.title || "New Dataset" %></h2>
		</div>
	</div>
	<div class="clear"></div>
	<% form_for([:admin, @dataset]) do |f| %>
		<div class="grid_6">
			<div class="box">
				  <%= f.error_messages %>
					<%= f.hidden_field :rev, :value => @dataset['_rev'] if @dataset['_rev'] %>
					<fieldset>
						<h5>General</h5>
				  	<p><%= f.label :title %> <%= f.text_field :title %></p>
						<%= render :partial => 'physical/metadatas/form', :locals => { :metadata => @metadata } %>
				</fieldset>
			</div>
		</div>
		<div class="grid_6">
			<div class="box">
				<fieldset>
				<h5>Properties</h5>
				<div id="property-definitions">
					<%= render( :partial => 'admin/property_definitions/form', 
											:collection => @dataset.properties,
											:spacer_template => "admin/property_definitions/spacer") %>
				</div>
				</fieldset>
			</div>
		</div>
		<p>
			<%= link_to 'Back', admin_datasets_path %>
			<%= f.submit "Submit", :class => "buttonSubmit", :style => "float: right;"%><br/>
		</p>
	<% end %>	
</div>
